import { DatePostsPipe } from './date-posts-pipe';

describe('DatePostsPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePostsPipe();
    expect(pipe).toBeTruthy();
  });
});
