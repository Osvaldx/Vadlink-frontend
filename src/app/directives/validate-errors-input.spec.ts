import { ValidateErrorsInput } from './validate-errors-input';

describe('ValidateErrorsInput', () => {
  it('should create an instance', () => {
    const directive = new ValidateErrorsInput();
    expect(directive).toBeTruthy();
  });
});
