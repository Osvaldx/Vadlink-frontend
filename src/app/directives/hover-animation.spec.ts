import { HoverAnimation } from './hover-animation';

describe('HoverAnimation', () => {
  it('should create an instance', () => {
    const directive = new HoverAnimation();
    expect(directive).toBeTruthy();
  });
});
