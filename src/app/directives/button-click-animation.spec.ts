import { ButtonClickAnimation } from './button-click-animation';

describe('ButtonClickAnimation', () => {
  it('should create an instance', () => {
    const directive = new ButtonClickAnimation();
    expect(directive).toBeTruthy();
  });
});
